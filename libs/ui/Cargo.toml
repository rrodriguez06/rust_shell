[package]
name = "lib_ui"
version = "0.1.0"
edition = "2021"

[dependencies]
tui = { version = "0.14", default-features = false, features = ['crossterm', 'serde'] }
unicode-width = "0.1.5"

lib_app = { path = "../app" }
